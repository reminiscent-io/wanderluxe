project_id = "arnengxblsfnezrqcsxw"
[functions.generate-trip-content]
verify_jwt = true

# Gateway JWT verification off, matching the other functions here: the body
# calls requireAuth() itself, which rejects the anon key that the gateway
# would otherwise wave through.
[functions.timezone-proxy]
verify_jwt = false

# Gateway JWT verification off because anonymous access is required, not merely
# tolerated: public trips on /explore/:slug render the map for logged-out
# visitors. The body does its own optional auth and rate-limits by IP when no
# bearer token is present.
[functions.place-coordinates-proxy]
verify_jwt = false